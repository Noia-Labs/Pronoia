"""Pronoia Model Lab domain package.

The package deliberately keeps model profiles, question banks and evaluation
jobs separate from the legacy backtest route.  Prediction tasks still produce
ordinary ``bt_runs`` so successful experiments remain usable by Arena.
"""
